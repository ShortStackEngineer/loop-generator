# Roadmap

> **Canonical version:**
> **<https://shortstackengineer.github.io/loop-generator/docs/roadmap.html>**
> This file is a stub — the site page is the full, maintained roadmap. Edit
> `site/docs/roadmap.html`, not this file.

The short shape of it:

- **Tier 1 (near-term, high leverage):** includes hardening the trust-guard
  defaults (`baseline`, `specGuard`, `evaluatorGuard`) to the audit posture the
  README recommends.
- **Tier 2 (medium-term, needs design).**
- **Deferred / not planned**, and what the project is explicitly *not* trying
  to solve (mis-specified checks, gamed metrics, context degradation, LLM
  unreliability on large novel work — the guards make these visible, not gone).
